# 前台项目开发规则

1. 产品规则以 `docs/PRD-v0.1.md` 为准。
2. v0.1 只实现登录、工作/个人空间、快速记录、收件箱和日记流。
3. 不加入 AI、目标管理、Web Push、打卡、积分、排行榜和逾期惩罚。
4. 工作与个人是服务端数据边界，不只是前端筛选。
5. 所有数据库查询必须使用当前登录用户限制数据范围。
6. 原始记录不能被自动改写。
7. 界面保持安静、克制，不用焦虑文案和红色告警。
8. 每次改动都要通过类型检查、Lint 和测试。
9. 保持 Docker Compose 可部署，不把密钥写入代码或镜像。
10. 不提前构建 v0.2 功能。
